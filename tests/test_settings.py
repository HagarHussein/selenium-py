


#change settings
def test_change_settings(browser):
    # GIVEN: the DuckduckGo home page is displayed
    # WHEN: the user searches for <phrase>
    # AND: the user changes one of the settings
    # THEN: the changes setting is applied
    raise Exception("Not Implemented Test")

#change region
def test_change_region(browser):
    # GIVEN: the DuckduckGo home page is displayed
    # WHEN: the user searches for <phrase>
    # AND: the user changes the region
    # THEN: the changed region is applied
    # AND: results are with this language
    raise Exception("Not Implemented Test")
